# Design semanal — 2026-09-23

Rodada `designer` #49 (op-agente). `OP_SECRET` e `EXPO_PUBLIC_SUPABASE_URL`
presentes; a rodada foi anotada normalmente.

## Peças renderizadas

- `pendente/2026-09-27-a-falta-que-voce-nao-sabe-se-cobra` — 7 quadros, sem
  captura pendente (`agenda-semanal-demonstracao` já existe). O quadro 4
  (`destaque`, no `imagens.md`) pedia as duas perguntas do app "uma embaixo
  da outra"; o tipo `destaque` só comporta um campo em evidência (ver
  cabeçalho de `render.mjs`), então usei `lista` com as duas perguntas como
  itens — mesmo texto, sem inventar nada. O quadro 5 (`captura`) com o
  parágrafo inteiro do `peca.md` estourou o rodapé na primeira
  renderização; encurtei o texto de apoio para "Falta é cobrável; cancelada
  não é. (Consultório de demonstração do Freud.)" e renderizei de novo —
  coube.
- `pendente/2026-09-28-a-mensagem-que-voce-reescreve-toda-vez` — 7 quadros,
  sem captura pendente (`cobranca-demonstracao` já existe). Sem outros
  ajustes de tipo; as chaves duplas `{{nome}}` etc. foram mantidas como o
  `imagens.md` pediu.
- `pendente/2026-09-29-a-semana-que-se-monta-sozinha` — 1 quadro (a capa do
  Reel, `capa`). Os seis trechos do roteiro são gravação de tela do dono,
  fora do escopo do Designer.
- `pendente/2026-09-30-linkedin-agenda-do-consultorio` — 1 quadro (`capa`),
  o cartão que acompanha o link do artigo.

Todos os PNGs foram conferidos (primeiro e último quadro de cada peça, e os
quadros que mudaram tipo/texto) — sem corte, sem título grande demais, sem
quadro vazio. `peca.md` de cada uma anotado com `arte: pronta (N quadros)`
(a de 29/09 como `arte: capa pronta (1 quadro)`, já que o corpo é vídeo).

## Peças puladas (sem arte a fazer)

- `pendente/2026-09-22-artigo-autorizacao-para-gravar` — artigo de blog, o
  molde `blog/_molde.html` não usa imagem.
- `pendente/2026-09-23-artigo-guarda-do-prontuario` — idem.
- `pendente/2026-09-25-email-cadastrou-e-nao-assinou` — sequência de
  e-mail, usa o envelope de `emailDrSig.ts`, sem PNG.
- `pendente/2026-09-26-artigo-agenda-do-consultorio` — idem aos outros
  artigos.
- Peças em `pendente/` com `quadros.json` e `arte: pronta` de rodadas
  anteriores (`2026-09-19`, `2026-09-20`, `2026-09-21`, `2026-09-24`) e as
  seis peças em `aprovado/` que já tinham arte — nenhuma mudança.

## Peça que não cabe no renderizador de hoje

- `pendente/2026-09-26-ficha-da-loja-google-play` — a ficha do Google Play
  pede imagens 1080×1920 com legenda dentro da imagem; `render.mjs` só
  produz 1080×1350. Não há tipo de quadro nem renderizador desse tamanho no
  repositório hoje. O próprio `imagens.md` da peça já descreve as três
  opções para o dono decidir (um `loja.mjs` novo no molde do `story.mjs`,
  um quadro `captura-loja` dentro do `render.mjs` atual, ou fazer à mão) e
  recomenda o `loja.mjs`; não mexi em `render.mjs` para resolver isso. As
  oito capturas de que a ficha precisa já existem em `marketing/capturas/`.

## Capturas pendentes (para o dono tirar na conta de demonstração)

Nenhuma captura faltou nas quatro peças renderizadas nesta rodada
(`agenda-semanal-demonstracao` e `cobranca-demonstracao` já existiam). A
única pendência de captura que segue em aberto, de rodada anterior, é
`nova-sessao-autorizacao.png` (Nova Sessão com o aviso "Autorização
necessária"), usada no roteiro do Reel de
`2026-09-21-autorizacao-do-comeco-ao-fim` — não é arte do Designer, é
gravação de tela do dono.

## O que não fiz

Não mudei texto de nenhuma peça, não usei captura fora de
`marketing/capturas/`, não publiquei nem movi pasta, não editei
`render.mjs`.
