# Design semanal — 2026-09-16

Rodada `designer` #22 (op-agente). `OP_SECRET` e `EXPO_PUBLIC_SUPABASE_URL`
presentes; a rodada foi anotada normalmente.

## Peças renderizadas

- `pendente/2026-09-19-as-sessoes-que-ficaram-sem-registro` — 7 quadros,
  sem captura pendente. Quadro 5 (`captura`) veio com título e texto longos
  demais e estourou o rodapé na primeira renderização; encurtei o título
  ("Ficam à vista.") e o texto sem mudar o sentido do pedido, e renderizei
  de novo — coube.
- `pendente/2026-09-20-perguntar-em-vez-de-procurar` — 7 quadros, sem
  captura pendente (a peça não usa captura, por decisão do próprio
  `imagens.md` — a Busca fica desligada na demonstração). O quadro 6 pedia
  4 cartões no tipo `cartoes`; esse tipo é feito para três cartões lado a
  lado (ver cabeçalho de `render.mjs`) e com 4 o último cartão saiu cortado
  fora da imagem. Troquei para o tipo `lista` (4 itens, o último em
  negrito para marcar o cartão que era "principal") — mesmo texto, sem
  overflow.
- `pendente/2026-09-21-autorizacao-do-comeco-ao-fim` — 1 quadro (a capa do
  Reel, `capa`). O vídeo em si é gravação de tela do dono, fora do escopo
  do Designer.
- `pendente/2026-09-24-linkedin-guarda-do-prontuario` — 1 quadro
  (`tipografia`).

Todos os PNGs foram conferidos (primeiro e último quadro de cada peça, e
os quadros que mudaram tipo/texto) — sem corte, sem título grande demais,
sem quadro vazio. `peca.md` de cada uma anotado com `arte: pronta (N
quadros)`.

## Peças puladas (sem arte a fazer)

- `pendente/2026-09-22-artigo-autorizacao-para-gravar` — artigo de blog, o
  molde `blog/_molde.html` não usa imagem.
- `pendente/2026-09-23-artigo-guarda-do-prontuario` — idem.
- `pendente/2026-09-25-email-cadastrou-e-nao-assinou` — sequência de
  e-mail, usa o envelope de `emailDrSig.ts`, sem PNG.
- Peças em `aprovado/` já tinham `quadros.json` e `arte: pronta` — nenhuma
  mudança.

## Capturas pendentes (para o dono tirar na conta de demonstração)

- `nova-sessao-autorizacao.png` — Nova Sessão com o aviso "Autorização
  necessária". Usada no roteiro do Reel de
  `2026-09-21-autorizacao-do-comeco-ao-fim` (não é arte do Designer, é
  gravação de tela do dono, mas a captura consta como pendência do
  `imagens.md` dessa peça).

Nenhuma outra captura faltou nas peças renderizadas nesta rodada.

## O que não fiz

Não mudei texto de nenhuma peça, não usei captura fora de
`marketing/capturas/`, não publiquei nem movi pasta, não editei
`render.mjs`.
